PKG_CPPFLAGS = -I C:/local323/include -I. -I../inst/include
## Use the R_HOME indirection to support installations of multiple R version
PKG_LIBS = -L C:/local323/lib $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" -e "RcppGSL:::LdFlags()")
